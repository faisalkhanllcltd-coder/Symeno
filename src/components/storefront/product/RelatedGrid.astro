---
const mockRelated = [
  { id: '1', name: 'Logitech MX Master 3S', price: 99.0 },
  { id: '2', name: 'Keychron K8 Pro', price: 119.0 },
];
---

<div class="mt-20 border-t border-gray-200 pt-16">
  <h2 class="mb-8 text-2xl font-bold tracking-tight text-gray-900 uppercase">
    Related Hardware.
  </h2>
  <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 md:grid-cols-4">
    {
      mockRelated.map((item) => (
        <a
          href={`/shop/product/${item.id}`}
          class="group block border border-gray-200 bg-[#F9FAFB] p-4 transition-colors hover:border-gray-400"
        >
          <div class="mb-4 flex aspect-square items-center justify-center bg-gray-100 font-mono text-[10px] text-gray-400">
            IMG
          </div>
          <h3 class="truncate text-sm font-bold text-gray-900 transition-colors group-hover:text-[#10b981]">
            {item.name}
          </h3>
          <p class="mt-1 font-mono text-sm text-gray-600">
            ${item.price.toFixed(2)}
          </p>
        </a>
      ))
    }
  </div>
</div>
