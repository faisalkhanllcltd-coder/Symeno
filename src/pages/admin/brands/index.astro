---
import AdminLayout from '../../../layouts/AdminLayout.astro';

if (!Astro.locals.user) return Astro.redirect('/auth/login');

const breadcrumbs = [{ label: 'Brands' }];

// Mock Data
const brands = [
  { id: 'BRD-01', name: 'Sony', status: 'Active', skuCount: 14 },
  { id: 'BRD-02', name: 'LG', status: 'Active', skuCount: 8 },
];
---

<AdminLayout
  title="Brand Matrix"
  subtitle="Supplier authorization controls"
  breadcrumbs={breadcrumbs}
  currentPath="/admin/brands"
  primaryAction={{ label: 'Add Brand', url: '/admin/brands/new' }}
>
  <div
    class="flex h-full flex-col overflow-hidden border border-white/10 bg-[#111318]"
  >
    <div class="flex-1 overflow-x-auto">
      <table class="w-full border-collapse text-left whitespace-nowrap">
        <thead>
          <tr
            class="border-b border-white/10 bg-black/40 font-mono text-[10px] tracking-widest text-white/40 uppercase"
          >
            <th class="px-4 py-4 font-normal">Brand ID</th>
            <th class="px-4 py-4 font-normal">Name</th>
            <th class="px-4 py-4 font-normal">Active SKUs</th>
            <th class="px-4 py-4 text-right font-normal">Status</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-white/[0.04]">
          {
            brands.map((brand) => (
              <tr class="group transition-colors hover:bg-white/[0.02]">
                <td class="px-4 py-3 font-mono text-xs text-white/60">
                  {brand.id}
                </td>
                <td class="px-4 py-3 text-xs font-bold text-white transition-colors group-hover:text-[#36f4a4]">
                  {brand.name}
                </td>
                <td class="px-4 py-3 font-mono text-xs text-white">
                  {brand.skuCount}
                </td>
                <td class="px-4 py-3 text-right">
                  <span class="inline-block border border-[#36f4a4]/30 bg-[#36f4a4]/10 px-2 py-1 text-[9px] font-bold tracking-widest text-[#36f4a4] uppercase">
                    {brand.status}
                  </span>
                </td>
              </tr>
            ))
          }
        </tbody>
      </table>
    </div>
  </div>
</AdminLayout>
