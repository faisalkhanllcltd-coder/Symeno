---
import AdminLayout from '../../../layouts/AdminLayout.astro';

if (!Astro.locals.user) return Astro.redirect('/auth/login');
const breadcrumbs = [
  { label: 'Products', url: '/admin/products' },
  { label: 'Provision Hardware' },
];
---

<AdminLayout
  title="Provision Hardware"
  subtitle="Form architecture secured"
  breadcrumbs={breadcrumbs}
  currentPath="/admin/products"
>
  <div class="border border-outline bg-base p-16 text-center">
    <p class="font-mono text-xs tracking-widest text-content-muted uppercase">
      Connect to API endpoint.
    </p>
  </div>
</AdminLayout>
